Mielui starts at version 0.1.0. The imported Sivir UI package was version 0.3.3; the two projects now have independent release numbers.

Use `@mielui/svelte` for imports and `mielui` for CLI commands. Sivir release notes are retained in `docs/upstream-changelog/` as historical reference, outside Mielui's active changelog. The imported component APIs are unchanged by this version reset.
