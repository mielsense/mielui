# Token Gap Report (Phase 0)

Empirically derived on 2026-05-29 from the working tree, not from estimates. Method:

- **Declared** = `--token:` declarations in `packages/mielui/src/ui.css` → 198 unique.
- **Emitted** = `--token:` lines inside the `themeToCss()` template literal in `packages/mielui/src/themes/presets.ts` → 129 unique.
- **Consumed** = `var(--token…)` **and** `'--token'` / `"--token"` string literals (JS reads via `getCssNumber`/`getCssDuration`) across **all** `.svelte`/`.ts` under `packages/mielui/src` except the two definition files → 182 unique.
- **Internal refs** = `var()` chains inside `ui.css`/`presets.ts` themselves (so intermediate tokens aren't misflagged as dead).

> Correction to the pre-audit "126 unused tokens / button colors hardcoded" claim: **false**. Buttons consume `var(--button-*-bg)` etc.; several "unused" motion tokens are consumed via **JS** (`getCssNumber(node, '--motion-sheet-offset')`, `getCssDuration(node, '--motion-duration-toast-in')`). The real gaps are smaller and different, below.

---

## Category A — Declared in `ui.css` but referenced nowhere (truly dead): 28

These produce no effect. Each is either a genuine dead token to delete, or evidence of a hardcode that bypasses it (cross-referenced to Category C).

| Token                                                                                                                                                                                             | Likely reason                                                                                                                       | Action                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `--button-primary-border`, `--button-success-border`, `--button-warning-border`, `--button-error-border`                                                                                          | Solid button variants never apply `border-[var(--button-*-border)]` (only flat/ghost/alternate set border color)                    | Wire border into solid variants **or** delete. Decision: wire (widens customizability).                                                                |
| `--button-outlined-shadow`                                                                                                                                                                        | `outlined` variant uses `--button-outlined-fancy-shadow`/`--field-outlined-shadow`, not this                                        | Delete (superseded).                                                                                                                                   |
| `--card-shadow`, `--card-highlight`                                                                                                                                                               | `card.svelte` markup applies no shadow/highlight class at all                                                                       | Wire `--card-shadow` into card; expose.                                                                                                                |
| `--checkbox-size`, `--checkbox-checked-bg`, `--checkbox-checked-foreground`                                                                                                                       | `checkbox.svelte` hardcodes `size-4`/`h-4 w-4` and `bg-primary` for the checked box                                                 | Wire tokens (see C).                                                                                                                                   |
| `--switch-thumb-bg`                                                                                                                                                                               | `switch.svelte:72` hardcodes `bg-white`                                                                                             | Wire token (see C).                                                                                                                                    |
| `--color-dialog`                                                                                                                                                                                   | overlays paint from `--color-overlay-bg` instead                                                                                    | Delete or repoint overlay tokens to it.                                                                                                                |
| `--color-muted`                                                                                                                                                                                   | no consumer                                                                                                                         | Delete or wire to a muted surface.                                                                                                                     |
| `--color-field-focus`                                                                                                                                                                             | field focus uses `--field-focus-border`, not a fill                                                                                 | Delete.                                                                                                                                                |
| `--color-floating-panel`, `--color-floating-panel-foreground`, `--floating-menu-item-foreground`, `--floating-menu-item-hover-bg`, `--floating-menu-item-active-bg`, `--floating-panel-highlight` | Generated for the `invertedPanels` feature but menu components read the non-`floating-` equivalents (`--menu-item-foreground` etc.) | **Inverted-panels feature is half-wired.** Repoint menu/popover consumers to the `--floating-*` tokens so `invertedPanels` works, or drop the feature. |
| `--panel-padding-lg`, `--space-control-y`, `--space-menu-item-y`                                                                                                                                  | intermediate spacing aliases never referenced                                                                                       | Delete.                                                                                                                                                |
| `--shadow-md`, `--shadow-lg`                                                                                                                                                                      | only `--shadow-xs`/`--shadow-sm` consumed                                                                                           | Keep as elevation scale (Phase 4 per-group elevation will consume them) or delete.                                                                     |
| `--font-mono`                                                                                                                                                                                     | used via Tailwind `font-mono` utility, not `var()`                                                                                  | Not a real gap (utility-driven).                                                                                                                       |
| `--text-base`                                                                                                                                                                                     | used via Tailwind `text-base`                                                                                                       | Not a real gap.                                                                                                                                        |
| `--motion-duration-tooltip`                                                                                                                                                                       | tooltip animates via `--motion-duration-overlay`/hardcode                                                                           | Wire tooltip to it, or delete.                                                                                                                         |

## Category B — Consumed by code but NOT emitted by `themeToCss` (live but not studio-editable): 77

The actionable core. Split by intent:

**B1 — Runtime / mechanism vars (must stay uneditable):** `--popover-available-height`, `--popover-available-width`, `--popover-trigger-width` (set by floating-ui), `--ui-button-shadow`, `--ui-field-bg`, `--ui-field-hover-bg`, `--ui-field-focus-border`, `--ui-field-foreground`, `--ui-field-placeholder`, `--ui-badge-shadow` (variant-local indirection), `--mielui-marquee-duration`, `--mielui-marquee-gap` (Marquee props). → **Exclude from Studio.**

**B2 — Derived color tokens that already track the palette** (`--button-{primary,secondary,success,warning,error,destructive,flat,ghost,outlined,alternate}-{bg,foreground,hover-bg,border}`, `--card-bg`, `--checkbox-bg`, `--color-field*`, `--color-panel-foreground`, `--color-tooltip`, `--color-tooltip-foreground`, `--toast-bg`, `--skeleton-base`, `--skeleton-highlight`, `--menu-item-*`, `--menu-label-foreground`, `--switch-track-*`, `--separator-color`). These follow semantic palette edits, so they "work" — but are not directly overridable. → **Expose a curated subset as advanced/group overrides (Phase 4); not all 77.**

**B3 — Genuine missing editable design axes (highest value):** `--field-padding-y` (only `-x` is emitted!), `--tooltip-padding-x`, `--tooltip-padding-y`, `--switch-track-padding`, `--button-gap`, `--button-height` (derives from `--size-control-md` but no independent control), `--field-focus-border`, `--field-disabled-opacity`, `--border-size`, `--outline-shadow`, `--panel-highlight`. → **Add to schema + emit + expose.**

## Category C — Hardcoded values in component markup that should be tokens

| File:line                                    | Hardcode                                                                                                     | Should be                                                                                   |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| `checkbox/checkbox.svelte:32,36,44,51`       | `size-4`, `h-4 w-4`, `bg-primary`, `duration-200`, `p-4 rounded-lg`                                          | `--checkbox-size`, `--checkbox-checked-bg`/`-foreground`, `--motion-duration-*`             |
| `switch/switch.svelte:72`                    | `bg-white` (thumb)                                                                                           | `--switch-thumb-bg`                                                                         |
| `card/card.svelte:20`                        | no shadow applied                                                                                            | `shadow-[var(--card-shadow)]`                                                               |
| `textarea/textarea.svelte:41`                | `min-h-[7rem]`, `py-[0.7rem]`                                                                                | `--textarea-min-height`, `--field-padding-y`                                                |
| `sheet/sheet-content.svelte:89,106`          | `150ms` backdrop in/out                                                                                      | `--motion-duration-overlay`                                                                 |
| `slider/slider.svelte:62,63`                 | `120ms` transform/box-shadow                                                                                 | `--motion-duration-hover` or new `--slider-*`                                               |
| `toast/toast.svelte:65,92,110`               | `size-[1.65rem]`, `h-7 px-2 text-[11.5px]`, `size-[1.2rem]`                                                  | toast icon-size / action-button tokens                                                      |
| `color-picker/color-picker.svelte:232,278,…` | `w-[244px]`, `h-6`, `text-[0.78rem]`                                                                         | color-picker layout tokens                                                                  |
| `shortcut/shortcut.svelte:55`                | `text-[11px]`                                                                                                | `--shortcut-font-size` or label scale                                                       |
| `button` `variants.ts:28,30`                 | `[--button-padding-x:0.75rem]` (sm), `[--button-padding-x:1rem]` (lg) **shadow the themed `buttonPaddingX`** | size-scoped padding tokens (`--button-padding-x-sm/-lg`) so themed padding drives all sizes |
| `avatar/avatar.svelte:15-18`                 | `size-7/9/12/16`, `text-[…]`                                                                                 | acceptable as fixed size variants; candidate for density/scale tokens (Phase 4)             |

## Category D — Emitted by `themeToCss` but consumed by no code (wasted generation): 16

`--button-outlined-shadow`, `--button-primary-border`, `--card-shadow`, `--color-floating-panel`, `--color-floating-panel-foreground`, `--color-dialog`, `--color-muted`, `--floating-menu-item-active-bg`, `--floating-menu-item-foreground`, `--floating-menu-item-hover-bg`, `--floating-panel-highlight`, `--font-mono`, `--motion-duration-tooltip`, `--motion-panel-easing`, `--shadow-lg`, `--shadow-md`.

Overlaps A. Notable: `--motion-panel-easing` is generated (a real motion-curve axis) but **no transition consumes it** — Phase 1 should wire panel/menu transitions to it (this is exactly the decoupled motion-curve direction the brief asks for). The `--floating-*` set confirms the half-wired `invertedPanels` feature.

---

## Summary of Phase 1 work implied

1. **Wire the half-wired feature:** make menu/popover components read `--floating-menu-item-*` so `invertedPanels` actually works (or remove).
2. **Eliminate Category C hardcodes**, especially: checkbox geometry/colors, switch thumb, card shadow, sheet/slider timings, textarea/toast/color-picker geometry, and the button sm/lg padding shadowing.
3. **Promote Category B3** (and a curated B2 subset) into the editable schema so they emit and become controllable.
4. **Wire `--motion-panel-easing`** into panel/menu transitions (decoupled motion curve).
5. **Resolve dead tokens (A/D):** wire the valuable ones (button borders, card shadow), delete the inert aliases.
6. Introduce the **group-scoped layer** (`--menu-*`, `--panel-*`, `--surface-*`, `--control-*`) with `var(--component, var(--group))` fallback so per-group + per-component override both work.
